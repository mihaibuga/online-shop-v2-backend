# Online Shop V2 Backend

Build Docker Image:

- Run the following command while inside the project's root folder:
    - "__docker build -t online-shop .__"


Run the Docker Image in a Container:

- Run the following command:
    - "__docker run online-shop__"